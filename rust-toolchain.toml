# Pin the Rust toolchain so cargo, rustfmt, and clippy are deterministic across
# machines and CI. rustfmt output drifts between releases — bump this version
# deliberately and reformat in the same commit when you do.
[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
